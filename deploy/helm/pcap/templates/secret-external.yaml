{{/*
No inline secrets — PCAP reads from Secrets referenced in `existingSecrets`.
Populate those Secrets with CSI Secret Store / ExternalSecret / SealedSecret.

Example (don't commit this to the GitOps repo):

apiVersion: v1
kind: Secret
metadata:
  name: pcap-github
  namespace: pcap
type: Opaque
stringData:
  token: ghp_REDACTED
---
apiVersion: v1
kind: Secret
metadata:
  name: pcap-llm
  namespace: pcap
type: Opaque
stringData:
  anthropic-api-key: sk-ant-REDACTED
  openai-api-key: sk-REDACTED
---
See docs/installation.md for the canonical bootstrap.
*/}}
